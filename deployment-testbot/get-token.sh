#!/bin/bash
# LocalSend does not use bearer token authentication.
# Access is controlled via an optional PIN set by the user (query param ?pin=...).
# No token is required to call the LocalSend API in default configuration.
echo ""
